{
  "name": "muslim-diary",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "serve": "vue-tsc --noEmit && vite",
    "dev": "vue-tsc --noEmit && vite build --mode development",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "axios": "^0.26.1",
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "@vitejs/plugin-vue": "^2.2.0",
    "@vue/tsconfig": "^0.1.3",
    "sass": "^1.49.9",
    "typescript": "^4.5.4",
    "vite": "^2.8.0",
    "vite-plugin-chrome-extension": "^0.0.7",
    "vue-tsc": "^0.29.8"
  }
}
